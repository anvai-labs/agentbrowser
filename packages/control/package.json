{
  "name": "@agentbrowser/control",
  "version": "1.9.1",
  "private": true,
  "type": "module",
  "description": "Shared human and agent authority with browser-independent application operations",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome check src/",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentbrowser/core": "workspace:*",
    "@agentbrowser/engine": "workspace:*",
    "@agentbrowser/protocol": "workspace:*"
  },
  "devDependencies": {
    "@agentbrowser/testkit": "workspace:*",
    "vitest": "^2.1.9"
  }
}

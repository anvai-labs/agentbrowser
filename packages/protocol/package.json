{
  "name": "@agentbrowser/protocol",
  "version": "1.0.0",
  "private": true,
  "description": "Protocol schemas and types for AgentBrowser",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.16"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}

{
  "name": "@agentbrowser/engine",
  "version": "1.0.0",
  "private": true,
  "description": "BrowserEngine interface and engine-neutral types",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@agentbrowser/protocol": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
